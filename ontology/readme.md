# Ontology

This directory contains the OWL ontology modules developed within OAKE.

The ontology is organised into:

- a stable conceptual core;
- independent domain-specific modules;
- ontology imports and alignments.

The ontology implementation follows the OAKE Modelling Principles.

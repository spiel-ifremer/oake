# Validation Shapes

This directory contains SHACL validation profiles.

SHACL shapes specify expected data quality constraints independently from the ontology.

Different applications may use different validation profiles while sharing the same ontology.
